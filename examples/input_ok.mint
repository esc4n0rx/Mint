program init.
  var age type int.
  var accepted type bool.
  var initial type char.
initialization.
  input(age).
  input(accepted).
  input(initial).
  write(age).
  write(accepted).
  write(initial).
endprogram.
