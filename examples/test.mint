program init.
  var bad_char type char = 'AB'.
  var bad_int type int = 2.5.
  var ok_float type float = 4.0.
  var ok_char type char = 'Z'.
initialization.
  write(bad_char).
  write(bad_int).
  write(ok_float + true).
  write(unknown_var).
endprogram.
