program init.
  var currentYear type int.
  var currentDate type string.
initialization.
  currentYear = (system.year).
  currentDate = (system.date).

  write(currentYear).
  write(currentDate).
endprogram.
